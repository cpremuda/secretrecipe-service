
var _ = require('lodash');
var dao = require('../dao/DAO');

module.exports = {

    retrieveRecipe : function (objectId, callback) {
    },

    addRecipe : function (recipeToAdd, callback) {
    }
};